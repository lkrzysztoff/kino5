export interface DialogData {
    title: number;
    buttonText: string;
    inputLabelText: string;
    importedDialogData: number;
    score:number;
  }

  export interface score {
    id:number,
    score:number
  }